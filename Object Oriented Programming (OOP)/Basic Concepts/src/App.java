public class App {
    public static void main(String[] args) {

        // Object: An instance of the class which may contain it's attributes and methods

        // Car myCar = new Car();
        // System.out.println(myCar.color);
        // System.out.println(myCar.make);
        // myCar.Drive();
        // myCar.Brake();

        // Constructors: Special Method that is called when an object is instantiated (Created)

        // Human human1 = new Human("Aman", 19, 68);
        // Human human2 = new Human("Zafar",22, 75);

        // System.out.println(human1.name + ", Your age is "+human1.age+" And you Weigh "+human1.weight+" kg");
        // System.out.println(human2.name + ", Your age is "+human2.age+" And you Weigh "+human2.weight+" kg");
        // human1.program();
        // human2.debug();

        // Local: Declared inside a method, visible only to that particular method
        // Global: Declared outside a method, but within the same class, visible to all parts of the class

        DiceRoller diceroller = new DiceRoller();
    }
}
